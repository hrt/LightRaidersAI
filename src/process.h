#ifndef PROCESS_H
#define PROCESS_H
#include <string>

const std::string PROCESS_SETTINGS = std::string("settings");
const std::string PROCESS_UPDATE = std::string("update game");
const std::string PRCOESS_ACTION = std::string("action move");

#endif